Предварительная подготовка (Context Setting)
Сначала дайте AI контекст проекта, чтобы он понимал стиль.

Промпт для инициализации:

Plaintext

I want to redesign my landing page (builditfast.ai) to look like a high-tech, AI-native dev studio.
Current Stack: Next.js (App Router), Tailwind CSS, TypeScript, Lucide React.
Desired Vibe: "Linear-like", dark mode, glassmorphism, neon accents, futuristic, high performance.
New Dependency: Please assume I have installed 'framer-motion' and 'clsx' 'tailwind-merge'.
Do not remove existing content, but wrap it in new high-end UI components.
Задача 1: Глобальные стили и Фон (The "Dark Mode" Foundation)
Нам нужно избавиться от белого/плоского фона и сделать глубокую, дорогую "темную тему".

Task Prompt:

Plaintext

TASK: Update Global Layout and Background

1. Modify `globals.css` and `layout.tsx`.
2. Set the default background to a deep dark shade (e.g., `bg-slate-950` or `#020617`).
3. Create a background component called `GridBackground.tsx`.
   - It should use a CSS radial-gradient mask to fade out at the edges.
   - It should display a subtle small grid pattern (white/10 opacity).
   - Add a slight "spotlight" effect at the top center that follows the mouse or stays fixed.
4. Update the global typography: Use 'Inter' or 'Geist Sans' with tight tracking (`tracking-tight`) for headings.
5. Apply this `GridBackground` to the main `layout.tsx` so it persists across the app.
Задача 2: Hero Section с "Живым кодом" (The Hook)
Заменяем статичный текст и картинку на динамику.

Task Prompt:

Plaintext

TASK: Redesign Hero Section with Typewriter Effect

1. Create a new component `HeroSection.tsx`.
2. Left Side (Text):
   - Headline: "Build your MVP in weeks, not months." using a `bg-clip-text` with a gradient from white to slate-400.
   - Subheadline: Animate it appearing with a slight delay (Framer Motion `initial={{ opacity: 0, y: 20 }}`).
   - Buttons: Create a `GlowButton` component. It should have a gradient border and an internal glow effect on hover.

3. Right Side (Visual):
   - Create a `CodeTypewriter.tsx` component.
   - Visually simulate a VS Code window (macOS traffic light dots on top, dark background).
   - Use a `useEffect` to simulate typing out code character by character.
   - The code content should be a TypeScript snippet showing an AI backend initialization (e.g., `const ai = new Builder(); await ai.deploy();`).
   - Add syntax highlighting colors (hardcoded spans) for keywords like `const`, `await`, `new`.
Задача 3: Стек технологий (Infinite Marquee)
Делаем бегущую строку логотипов.

Task Prompt:

Plaintext

TASK: Create Infinite Tech Stack Marquee

1. Create a component `TechStackMarquee.tsx`.
2. Use `framer-motion` to animate a list of logos (React, Python, Next.js, PostgreSQL, OpenAI, Stripe) moving horizontally from right to left infinitely.
3. Style:
   - All logos should be monochrome (grayscale) and opacity-50 by default.
   - On hover: Scale up slightly (1.1), turn to full color, and opacity-100.
   - Add a gradient mask on the left and right edges of the container so logos fade in and out smoothly.
Задача 4: Bento Grid для преимуществ (Features)
Превращаем список услуг в красивые стеклянные карточки.

Task Prompt:

Plaintext

TASK: Implement Bento Grid for Features

1. Create a `BentoGrid.tsx` and `BentoCard.tsx` component.
2. Layout: Use CSS Grid to create a non-uniform layout (some cards span 2 columns, some 1).
3. Card Style (Glassmorphism):
   - Background: `bg-white/5` (extremely subtle).
   - Border: `border-white/10`.
   - Backdrop filter: `backdrop-blur-md`.
4. Hover Effect:
   - Implement a "Hover Border Gradient" or a "Mouse Follower" effect. When the mouse moves over the grid, a radial gradient glow should reveal the border of the cards nearby.
5. Content: Move the existing "AI-Accelerated", "Fixed Price", and "Security" content into these cards. Add Lucide icons for each.
Задача 5: Интерактивный процесс (Pipeline Animation)
Визуализируем путь от идеи до релиза.

Task Prompt:

Plaintext

TASK: Create Animated Process Pipeline

1. Create a component `ProcessPipeline.tsx`.
2. Design a vertical or horizontal timeline.
3. Connect the steps (Call -> Spec -> Dev -> Deploy) with a SVG line.
4. Animation:
   - Use Framer Motion to animate a "beam" of light (a path with a gradient stroke) traveling along the SVG line from start to finish repeatedly.
   - When the beam hits a step node (circle), the node should "pulse" or light up.
5. Each step should have a title and description that fades in when scrolled into view.
Финальная полировка (CTA & Footer)
Task Prompt:

Plaintext

TASK: Redesign CTA and Footer

1. CTA Section:
   - Create a large "Magnetic" button for "Start Project".
   - Background: A deep gradient (violet to blue) with low opacity.
2. Footer:
   - Clean up the layout.
   - Add a "Status: All Systems Operational" indicator with a pulsing green dot (classic dev studio trope).
   - Ensure the copyright and links use a muted text color (`text-slate-500`).